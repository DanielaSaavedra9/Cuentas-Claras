// Menú de navegación, Bloque 2: el tab "Aprender" está deshabilitado —
// RF06 (glosario) está fuera de alcance de esta entrega. Este archivo
// existe solo porque Expo Router exige un componente por cada
// Tabs.Screen; nunca se renderiza porque app/(tabs)/_layout.tsx
// intercepta el tabPress y no navega hacia acá.
export default function AprenderScreen() {
  return null;
}
